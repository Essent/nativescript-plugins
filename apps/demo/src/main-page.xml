<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon="" class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="nativescript-adobe-experience-cloud" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-appdynamics" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-iadvize" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-medallia" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-ng-sentry" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-ui-charts" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-urban-airship" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>